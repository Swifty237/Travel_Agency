import { Text, View } from "react-native";

const backImg = require("../assets/backimg_contact.png")

export default function Contact() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>Ok Contact</Text>
        </View>
    );
}
